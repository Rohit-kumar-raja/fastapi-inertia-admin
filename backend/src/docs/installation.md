# Installation

1. Clone the repository
```bash
git clone <repository-url>
```

2. Change directory
```bash
cd <repository-name>
```

3. Create .env file
```bash
cp .env.example .env
```

4. Adjust .env file as per requirement

5. Start the project
```bash
docker-compose up  --build
```
